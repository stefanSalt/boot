<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lee.questionnaire.system.mapper.QuestionnaireTemplateMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.lee.questionnaire.system.model.entity.QuestionnaireTemplate">
        <id column="id" property="id" />
        <result column="title" property="title" />
        <result column="description" property="description" />
        <result column="creator_id" property="creatorId" />
        <result column="category" property="category" />
        <result column="cover_image" property="coverImage" />
        <result column="questions_json" property="questionsJson" />
        <result column="is_public" property="isPublic" />
        <result column="use_count" property="useCount" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="deleted" property="deleted" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, title, description, creator_id, category, cover_image, questions_json, is_public, use_count, create_time, update_time, deleted
    </sql>


    <!-- 问卷模板表分页列表 -->
    <select id="listPagedQuestionnaireTemplates" resultType="com.lee.questionnaire.system.model.entity.QuestionnaireTemplate">
        SELECT
            <include refid="Base_Column_List"/>
        FROM
            questionnaire_template
        <where>
<!--            <if test='queryParams.name!=null and queryParams.name.trim() neq ""'>-->
<!--                AND (-->
<!--                    name LIKE CONCAT('%',#{queryParams.name},'%')-->
<!--                )-->
<!--            </if>-->
        </where>
        ORDER BY id DESC
    </select>

</mapper>
