<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lee.questionnaire.system.mapper.QuestionOptionMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.lee.questionnaire.system.model.entity.QuestionOption">
        <id column="id" property="id" />
        <result column="question_id" property="questionId" />
        <result column="content" property="content" />
        <result column="sort" property="sort" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, question_id, content, sort, create_time, update_time
    </sql>
    <insert id="saveBatch" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="id">

        insert into question_option (question_id, content, sort) values
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.questionId}, #{item.content}, #{item.sort})
        </foreach>
    </insert>
    <delete id="deleteByQuestionId">
        delete from question_option where question_id = #{id}
    </delete>
    <delete id="deleteByQuestionIds">
        delete from question_option where question_id in
        <foreach collection="list" item="item" index="index" open="(" close=")" separator=",">
            #{item}
        </foreach>
    </delete>


    <!-- 选项表分页列表 -->
    <select id="listPagedQuestionOptions" resultType="com.lee.questionnaire.system.model.entity.QuestionOption">
        SELECT
            <include refid="Base_Column_List"/>
        FROM
            question_option
        <where>
            <if test='queryParams.name!=null and queryParams.name.trim() neq ""'>
                AND (
                    name LIKE CONCAT('%',#{queryParams.name},'%')
                )
            </if>
        </where>
        ORDER BY id DESC
    </select>

</mapper>
