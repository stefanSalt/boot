<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lee.lecture.system.mapper.NewsCategoryMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.lee.lecture.system.model.entity.NewsCategory">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="sort" property="sort" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, sort, status, create_time, update_time
    </sql>


    <!-- 资讯分类表分页列表 -->
    <select id="listPagedNewsCategorys" resultType="com.lee.lecture.system.model.entity.NewsCategory">
        SELECT
            <include refid="Base_Column_List"/>
        FROM
            news_category
        <where>
            <if test='queryParams.name!=null and queryParams.name.trim() neq ""'>
                AND (
                    name LIKE CONCAT('%',#{queryParams.name},'%')
                )
            </if>
        </where>
        ORDER BY sort
    </select>
    <select id="listNewsCategoryOptions" resultType="com.lee.lecture.system.model.entity.NewsCategory">
        SELECT id, name FROM news_category
        WHERE status = 1
        ORDER BY sort
    </select>

</mapper>
