<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lee.warehouse.system.mapper.WarehouseMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.lee.warehouse.system.model.entity.Warehouse">
        <id column="id" property="id" />
        <result column="manager" property="manager" />
        <result column="name" property="name" />
        <result column="location" property="location" />
        <result column="area" property="area" />
        <result column="type" property="type" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, manager, name, location, area, type, create_time, update_time
    </sql>


    <!-- 仓库信息分页列表 -->
    <select id="listPagedWarehouses" resultType="com.lee.warehouse.system.model.entity.Warehouse">
        SELECT
            <include refid="Base_Column_List"/>
        FROM
            warehouse
        <where>
            <if test='queryParams.name!=null and queryParams.name.trim() neq ""'>
                AND (
                    name LIKE CONCAT('%',#{queryParams.name},'%')
                )
            </if>
        </where>
        ORDER BY id DESC
    </select>
    <select id="listWarehouseOptions" resultType="com.lee.warehouse.system.model.entity.Warehouse">
        SELECT
            id, name
        FROM
            warehouse
        ORDER BY id DESC
    </select>

</mapper>
