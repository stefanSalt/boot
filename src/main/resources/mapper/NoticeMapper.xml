<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lee.selection.system.mapper.NoticeMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.lee.selection.system.model.entity.Notice">
        <id column="id" property="id" />
        <result column="title" property="title" />
        <result column="detail" property="detail" />
        <result column="creator" property="creator" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, title, detail, creator, create_time, update_time
    </sql>


    <!-- 公告信息分页列表 -->
    <select id="listPagedNotices" resultType="com.lee.selection.system.model.entity.Notice">
        SELECT
            <include refid="Base_Column_List"/>
        FROM
            notice
        <where>
            <if test='queryParams.title!=null and queryParams.title.trim() neq ""'>
                AND (
                    title LIKE CONCAT('%',#{queryParams.title},'%')
                )
            </if>
        </where>
        ORDER BY id DESC
    </select>

</mapper>
