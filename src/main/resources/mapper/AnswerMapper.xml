<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lee.questionnaire.system.mapper.AnswerMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.lee.questionnaire.system.model.entity.Answer">
        <id column="id" property="id" />
        <result column="response_id" property="responseId" />
        <result column="question_id" property="questionId" />
        <result column="option_id" property="optionId" />
        <result column="option_ids" property="optionIds" />
        <result column="text_answer" property="textAnswer" />
        <result column="score" property="score" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, response_id, question_id, option_id, option_ids, text_answer, score, create_time
    </sql>


    <!-- 答案表分页列表 -->
    <select id="listPagedAnswers" resultType="com.lee.questionnaire.system.model.entity.Answer">
        SELECT
            <include refid="Base_Column_List"/>
        FROM
            answer
        <where>
            <if test='queryParams.name!=null and queryParams.name.trim() neq ""'>
                AND (
                    name LIKE CONCAT('%',#{queryParams.name},'%')
                )
            </if>
        </where>
        ORDER BY id DESC
    </select>

</mapper>
